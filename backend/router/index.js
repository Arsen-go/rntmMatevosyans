const { saveUserMessage } = require("./rntm-message-router");
const { saveUser, addOneVisitor } = require("./user-router");

module.exports = {
    saveUserMessage,
    saveUser,
    addOneVisitor,
};
